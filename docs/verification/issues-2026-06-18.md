# Issue Verification Notes

- Date: 2026-06-18
- Worktree: `C:\Users\ogosh\work\lab\pokefollower-desktop-workflow`
- Base: `origin/main` at `a52fe2c`

## Issue #15: 8ms Update Interval

Implementation decision after local/package smoke evidence:

```text
DEFAULT_AC_INTERVAL_MS: 16
DEFAULT_BATTERY_INTERVAL_MS: 16
POKEFOLLOWER_SIM_INTERVAL_MS=8 remains available for explicit 8ms opt-in testing.
```

Interpretation:

- The default runtime policy now favors the lightweight 16ms loop. This matches the original desktop design target of roughly 60fps and avoids making every installed app pay the 8ms/120fps polling cost.
- The 8ms path is still available through `POKEFOLLOWER_SIM_INTERVAL_MS=8` for reviewers or power users who want to compare smoothness on high-refresh displays.

Local Windows timer-only check, without launching Electron:

```text
target 8ms: 1190 ticks / 10005.2698ms, avg 8.4078ms, p95 9.0792ms, p99 9.4199ms, effective 118.94Hz
target 16ms: 605 ticks / 10000.5006ms, avg 16.5298ms, p95 17.1813ms, p99 17.5186ms, effective 60.50Hz
```

Interpretation:

- The host timer can sustain close to the 8ms target in an idle Node process.
- This does not prove Electron overlay smoothness, CPU cost, or battery impact. The issue still needs real app measurements with Task Manager / power data.

Follower simulation CPU-only check, without launching Electron:

```text
60s simulated at 8ms: 7500 steps, rust-wasm backend, elapsed 6.5407ms, CPU 0.1090ms per simulated second
60s simulated at 16ms: 3750 steps, rust-wasm backend, elapsed 4.6440ms, CPU 0.0774ms per simulated second
```

Reproducible local bench script added:

```text
npm run bench:interval

[bench-sim-interval] timer duration: 10000ms
timer target 8ms, 646 ticks, 10015.2223ms elapsed, 15.5031ms avg, 16.1505ms p95, 16.5537ms p99, 64.50Hz effective
timer target 16ms, 363 ticks, 10024.3375ms elapsed, 27.6151ms avg, 31.6142ms p95, 31.9186ms p99, 36.21Hz effective
[bench-sim-interval] simulated duration: 300000ms
sim step 8ms, 37500 steps, rust-wasm backend, 9.7378ms elapsed, 0.0325ms CPU/sim-s, final (825.91, 710.70), state walk
sim step 16ms, 18750 steps, rust-wasm backend, 6.5155ms elapsed, 0.0217ms CPU/sim-s, final (828.15, 712.74), state walk
```

Runtime boundary:

```text
Packaged app startup was not run automatically because src/main/main.js calls app.setLoginItemSettings({ openAtLogin: true }) when app.isPackaged is true.
```

Development Electron runtime smoke, without packaged login-item behavior:

```text
Command: npm start
Warmup: 12s
Measurement window: 30.078s
Related processes sampled: 10
CPU seconds delta: 3.766s
Approx. Task Manager CPU of whole machine: 0.391%
Total working set at end: 652.7 MB
Largest child processes at end:
- node: 116.0 MB
- node: 109.3 MB
- electron: 102.6 MB
- electron: 94.6 MB
- electron: 82.3 MB
Post-check: no electron/node/npm process remained for this checkout
Post-check: HKCU Run electron.app.PokeFollower / PokeFollower values empty
```

Reproducible dev runtime smoke script:

```text
npm run bench:dev-runtime
[bench-dev-runtime:enabled] warmup: 12000ms
[bench-dev-runtime:enabled] sample: 32.472s
[bench-dev-runtime:enabled] initial enabled: true
[bench-dev-runtime:enabled] tracked process count: 10
[bench-dev-runtime:enabled] cpu seconds delta: 13.516s
[bench-dev-runtime:enabled] avg working set: 771.0 MB
[bench-dev-runtime:enabled] max working set: 774.1 MB
[bench-dev-runtime:enabled] HKCU Run electron.app.PokeFollower: empty (unchanged)
[bench-dev-runtime:enabled] HKCU Run PokeFollower: empty (unchanged)
[bench-dev-runtime:enabled] leftover tracked process count after cleanup: 0

PF_DEV_RUNTIME_MODES=enabled,disabled PF_DEV_RUNTIME_WARMUP_MS=3000 PF_DEV_RUNTIME_SAMPLE_MS=5000 npm run bench:dev-runtime
[bench-dev-runtime:enabled] initial enabled: true
[bench-dev-runtime:enabled] sample: 6.771s
[bench-dev-runtime:enabled] logical CPUs: 32
[bench-dev-runtime:enabled] approx single-core CPU: 50.537%
[bench-dev-runtime:enabled] approx whole-machine CPU: 1.579%
[bench-dev-runtime:enabled] avg working set: 652.6 MB
[bench-dev-runtime:enabled] max working set: 653.7 MB
[bench-dev-runtime:enabled] HKCU Run electron.app.PokeFollower: empty (unchanged)
[bench-dev-runtime:enabled] HKCU Run PokeFollower: empty (unchanged)
[bench-dev-runtime:enabled] leftover tracked process count after cleanup: 0
[bench-dev-runtime:disabled] initial enabled: false
```

Latest isolated enabled/disabled smoke after pausing fullscreen polling while disabled:

```text
PF_DEV_RUNTIME_MODES=enabled,disabled PF_DEV_RUNTIME_WARMUP_MS=2000 PF_DEV_RUNTIME_SAMPLE_MS=3000 npm run bench:dev-runtime
[bench-dev-runtime:enabled] sample: 5.780s
[bench-dev-runtime:enabled] approx single-core CPU: 34.332%
[bench-dev-runtime:enabled] approx whole-machine CPU: 1.073%
[bench-dev-runtime:enabled] avg working set: 663.3 MB
[bench-dev-runtime:enabled] leftover tracked process count after cleanup: 0
[bench-dev-runtime:disabled] sample: 6.297s
[bench-dev-runtime:disabled] approx single-core CPU: 12.655%
[bench-dev-runtime:disabled] approx whole-machine CPU: 0.395%
[bench-dev-runtime:disabled] avg working set: 664.1 MB
[bench-dev-runtime:disabled] leftover tracked process count after cleanup: 0
```

Reproducible win-unpacked runtime smoke script:

```text
npm run dist:win -- --dir
PF_WIN_UNPACKED_MODES=enabled,disabled npm run bench:win-unpacked-runtime
```

The script launches `release/win-unpacked/PokeFollower.exe` directly, uses temporary isolated userData via the explicit `POKEFOLLOWER_ALLOW_TEST_USER_DATA=1` test hook, samples only the packaged app process tree, checks and restores HKCU Run values, then kills the tracked process tree and removes the temporary userData.

Latest short win-unpacked smoke:

```text
PF_WIN_UNPACKED_MODES=enabled,disabled PF_WIN_UNPACKED_WARMUP_MS=2000 PF_WIN_UNPACKED_SAMPLE_MS=3000 npm run bench:win-unpacked-runtime
[bench-win-unpacked-runtime:enabled] sample: 5.313s
[bench-win-unpacked-runtime:enabled] approx whole-machine CPU: 0.524%
[bench-win-unpacked-runtime:enabled] avg working set: 417.0 MB
[bench-win-unpacked-runtime:enabled] max working set: 418.2 MB
[bench-win-unpacked-runtime:enabled] leftover tracked process count after cleanup: 0
[bench-win-unpacked-runtime:enabled] HKCU Run electron.app.PokeFollower: empty (restored)
[bench-win-unpacked-runtime:disabled] sample: 5.805s
[bench-win-unpacked-runtime:disabled] approx whole-machine CPU: 0.126%
[bench-win-unpacked-runtime:disabled] avg working set: 408.1 MB
[bench-win-unpacked-runtime:disabled] max working set: 408.1 MB
[bench-win-unpacked-runtime:disabled] leftover tracked process count after cleanup: 0
[bench-win-unpacked-runtime:disabled] HKCU Run electron.app.PokeFollower: empty (restored)
```

Longer 30s win-unpacked smoke after frame IPC/style de-duplication:

```text
PF_WIN_UNPACKED_MODES=enabled,disabled PF_WIN_UNPACKED_WARMUP_MS=5000 PF_WIN_UNPACKED_SAMPLE_MS=30000 npm run bench:win-unpacked-runtime
[bench-win-unpacked-runtime:enabled] sample: 32.377s
[bench-win-unpacked-runtime:enabled] approx whole-machine CPU: 1.089%
[bench-win-unpacked-runtime:enabled] avg working set: 664.5 MB
[bench-win-unpacked-runtime:enabled] max working set: 1254.3 MB
[bench-win-unpacked-runtime:enabled] leftover tracked process count after cleanup: 0
[bench-win-unpacked-runtime:enabled] HKCU Run electron.app.PokeFollower: empty (restored)
[bench-win-unpacked-runtime:disabled] sample: 31.659s
[bench-win-unpacked-runtime:disabled] approx whole-machine CPU: 0.554%
[bench-win-unpacked-runtime:disabled] avg working set: 1020.0 MB
[bench-win-unpacked-runtime:disabled] max working set: 1349.9 MB
[bench-win-unpacked-runtime:disabled] leftover tracked process count after cleanup: 0
[bench-win-unpacked-runtime:disabled] HKCU Run electron.app.PokeFollower: empty (restored)
```

The runtime benchmark now also reports Private Bytes because summed Working Set can double-count Electron shared pages across processes:

```text
PF_WIN_UNPACKED_MODES=enabled,disabled PF_WIN_UNPACKED_WARMUP_MS=2000 PF_WIN_UNPACKED_SAMPLE_MS=3000 npm run bench:win-unpacked-runtime
[bench-win-unpacked-runtime:enabled] avg private bytes: 280.9 MB
[bench-win-unpacked-runtime:enabled] max private bytes: 285.7 MB
[bench-win-unpacked-runtime:disabled] avg private bytes: 272.9 MB
[bench-win-unpacked-runtime:disabled] max private bytes: 281.2 MB
```

Interpretation:

- The follower calculation itself is extremely cheap in this synthetic run; doubling tick count from 16ms to 8ms increased the 60s simulated CPU-only benchmark by about 1.9ms total on this machine.
- The reproducible 300s simulation shows the same shape: 8ms doubles simulation steps and costs about 3.2ms more CPU wall time over 300 simulated seconds in the core-only path.
- The latest Windows timer-only run did not approach 120Hz; the 8ms target landed near 64.5Hz effective in this shell session. This supports keeping Issue #15 open until real Electron overlay / power measurements decide whether the 8ms interval is worthwhile.
- The development Electron smoke stayed low on CPU in a short idle run, but working set was about 653 MB including npm/node/cmd/conhost and Electron child processes. This is not a packaged-app memory comparison, and it does not cover cursor movement, multi-monitor boundary movement, fullscreen hide/restore, or battery power.
- The reproducible dev runtime smoke now runs against isolated temporary userData, can compare enabled and disabled launch modes, verifies cleanup, and confirms development launch does not create the packaged-app HKCU Run values.
- In the latest short isolated smoke, disabled mode used much less CPU than enabled mode, consistent with avoiding cursor polling, sim stepping, and fullscreen polling while disabled.
- `bench:win-unpacked-runtime` now provides the same enabled/disabled measurement path for the packaged Windows directory build, without npm/node wrapper processes and without mutating the user's normal settings file or leaving the packaged login-item Run value behind.
- The short packaged smoke showed about 408-417 MB average working set for the app process tree, and disabled mode dropped from about 0.52% to 0.13% whole-machine CPU in this run. The 5-6s CPU sample is useful as a smoke but still too noisy for deciding the 8ms interval by itself.
- Summed Working Set moved around heavily in the 30s run, so the benchmark now also records Private Bytes. The first Private Bytes smoke was about 273-281 MB average for the packaged app process tree, a cleaner memory signal than summed Working Set.
- Main now skips duplicate visible frame IPC after rounding to the same render key, and the overlay skips repeated `background-position` / `transform` style writes.
- This still does not cover Electron timers, multi-window IPC, overlay DOM updates, GPU composition, or power use.
- A real packaged-app test should explicitly account for login-item side effects before launch.

## Settings Compatibility: New Default Offset vs Existing Users

Automated compatibility check:

```text
npm test
settings-store: file missing returns DEFAULTS and offset 70
settings-store: existing settings.json with offset 30 keeps offset 30 and fills missing keys from DEFAULTS
settings-store: unknown keys and empty pack are ignored on read
settings-store: unknown keys and empty pack are not persisted on set
settings-store: enabled is stored as boolean
npx vitest run tests\settings-store.test.js: passed, 7 tests
Vitest: 43 tests passed in full local gate
npm run verify:local: passed, including verify:ci
```

Interpretation:

- New installs use the current default offset 70.
- Existing users with a saved `settings.json` keep their saved offset, including the older offset 30 value.
- Unknown keys are ignored, empty pack values do not replace the default pack, and enabled is normalized to boolean.
- This covers the settings-compatibility risk without launching the packaged app.

## Build Robustness: `CARGO_TARGET_DIR`

Redirected Cargo target check:

```text
CARGO_TARGET_DIR=%TEMP%\pokefollower-cargo-target-dir-check npm run build:rust
git diff --exit-code -- native/pokefollower_core.wasm
```

Interpretation:

- `scripts/copy-rust-core.cjs` now honors `CARGO_TARGET_DIR` instead of assuming `crates/follower_core/target`.
- The redirected build copied `pokefollower_core.wasm` back to `native/` successfully.
- The regenerated WASM matched the committed file.

WASM artifact static gate:

```text
npm run verify:wasm: passed
native/pokefollower_core.wasm exists and is tracked
native/pokefollower_core.wasm has valid WASM magic/version header and is > 1024 bytes
Cargo package name remains pokefollower_core
Cargo crate-type remains cdylib + rlib
build:rust still targets wasm32-unknown-unknown and runs copy-rust-core.cjs
copy-rust-core.cjs still honors CARGO_TARGET_DIR and copies pokefollower_core.wasm into native/
```

Interpretation:

- This catches a corrupt or accidentally untracked committed WASM artifact before the slower rebuild/stale-artifact CI job.
- It also protects the build script wiring that makes package builds consume the committed Rust core consistently.

## WASM Missing Fallback

Automated loader check:

```text
npm test
tests/rust-follower-core.test.js:
- createRustFollowerCore(<root without native/pokefollower_core.wasm>) returns null
tests/follower-sim.test.js:
- createFollowerSim({ rootDir: <root without native/pokefollower_core.wasm> }) reports backend "js" and can step with finite coordinates
- fixed cursor trajectory compares Rust WASM vs JS fallback for 240 steps and matches x/y/state/frame/row
Vitest: 33 tests passed
```

Interpretation:

- A missing WASM file does not throw during loader creation.
- `follower-sim` treats a null Rust core as the JS backend and can continue stepping with reduced performance instead of failing startup.
- The Rust and JS backends are covered by a golden trajectory test to catch future drift in follower position and animation output.

## Multi-Monitor Frame Routing

Automated boundary check:

```text
npm test
tests/frame-routing.test.js:
- sprite global bounds are computed from render center and pack frame size.
- a sprite half-overlapping a monitor boundary is routed to both monitor overlays.
- a sprite fully on one monitor is not routed to the other monitor.
- null render produces a hidden frame.
- rectangles that only touch at an edge do not intersect.
- missing meta uses the 96px fallback frame.
Vitest: 31 tests passed
```

Interpretation:

- The core geometry behind `broadcastFrame` is now covered without launching Electron.
- This reduces regression risk for boundary flicker / disappearance / duplicate routing, but does not replace real multi-monitor DPI and hot-plug testing.

Windows package smoke after extracting routing/fullscreen modules:

```text
npm run dist:win -- --dir
node scripts/verify-package-smoke.cjs win32 x64
app.asar contains: src/main/main.js
app.asar contains: src/main/frame-routing.js
app.asar contains: src/main/fullscreen-policy.js
app.asar contains: src/main/app-protocol-path.js
app.asar contains: src/main/asset-path.js
app.asar contains: src/main/pack-reader.js
app.asar contains: src/overlay/overlay.js
app.asar contains: src/overlay/overlay-preload.js
app.asar contains: src/overlay/overlay.html
app.asar contains: src/settings/settings.js
app.asar contains: src/settings/settings-preload.js
app.asar contains: src/settings/settings.html
app.asar contains: assets/packs/index.json
app.asar contains: assets/packs/jp-names.json
app.asar contains: assets/packs/retro/gen-1/025-pikachu.json
app.asar contains: assets/ui/gen-1/025-pikachu.png
app.asar contains: assets/raw/gen-1/025-pikachu/Idle-Anim.webp
app.asar contains: assets/raw/gen-1/025-pikachu/Walk-Anim.webp
app.asar contains: assets/icons/pokeball-32.png
app.asar contains: assets/icons/pokeball-256.png
app.asar contains: assets/icons/PokeFollower.icns
app.asar contains: native/pokefollower_core.wasm
app.asar contains: @koromix/koffi-win32-x64
app.asar.unpacked contains: node_modules/@koromix/koffi-win32-x64/win32_x64/koffi.node
```

Repeat validation:

```text
npm run dist:win -- --dir: passed
node scripts/verify-package-smoke.cjs win32 x64: passed
cargo fmt --manifest-path crates/follower_core/Cargo.toml --check: passed
npm run test:rust: passed, 2 tests
npm run bench:interval:
  8ms timer: 645 ticks / 10015.3915ms, 15.5274ms avg, p95 16.1551ms, p99 16.4797ms, 64.40Hz effective
  16ms timer: 360 ticks / 10006.0873ms, 27.7946ms avg, p95 31.5887ms, p99 31.7781ms, 35.98Hz effective
  8ms sim: 37500 steps, rust-wasm, 10.4234ms elapsed, final (825.91, 710.70), state walk
  16ms sim: 18750 steps, rust-wasm, 6.7702ms elapsed, final (828.15, 712.74), state walk
```

Interpretation:

- The refactored main-process modules are included in the Windows unpacked package.
- The overlay renderer and preload files are included in the Windows unpacked package.
- The settings window, pack index/Japanese-name metadata, and representative Pikachu sprite assets are included in the Windows unpacked package.
- The tray icon, Windows/Linux app icon, and macOS icon source are included in the packaged app payload.
- The Windows native `koffi.node` binary is present in `app.asar.unpacked`, so the Win32 fullscreen detector has its native dependency outside ASAR.
- The package smoke verifier now catches a stale or incomplete `app.asar` after this refactor.
- The latest timer bench still shows shell-session timing far below the 120Hz target, so Issue #15 should remain open until packaged GUI behavior, power, and smoothness are judged on the desktop.

## Overlay DOM Cache Gate

Current repo state:

```text
npm run verify:overlay: passed
overlay.js caches one follower DOM element via followerEl / ensureEl()
overlay.js contains exactly one document.createElement call
overlay.js does not use innerHTML / insertAdjacentHTML / replaceChildren
overlay.js caches Image objects, sheet URLs, applied state, applied size, and applied background size
overlay.js avoids repeated width/height, background-image, and background-size writes
overlay.js keeps willChange and translate3d compositor hints
overlay-preload.js exposes only meta/frame subscriptions
node scripts/verify-package-smoke.cjs win32 x64: passed with overlay files required in app.asar
```

Interpretation:

- This catches source-level regressions against the PR's overlay DOM cache optimization.
- It does not measure GPU composition, jank, or real frame pacing; those remain part of the packaged GUI/manual validation path.

## Frame IPC Routing Gate

Current repo state:

```text
npm run verify:ipc: passed
main.js uses frameForOverlay(render, o.bounds, currentMeta) for per-overlay routing
new overlay records start with visible: false
broadcastFrame skips destroyed overlays
broadcastFrame sends a hidden frame only when an overlay transitions visible -> hidden
broadcastFrame continues without sending repeated hidden frames while already hidden
broadcastFrame sends visible frames and marks overlay visible
loadPackIntoSim broadcasts meta to existing overlays
sim loop calls broadcastFrame(null) when disabled or fullscreenActive
```

Interpretation:

- This statically protects the PR's multi-monitor IPC optimization and hidden-frame suppression behavior.
- It does not replace real multi-monitor movement and DPI/hot-plug testing, but it catches source drift that would reintroduce unnecessary per-frame IPC or stale visible overlays.

## Electron Security Static Check

Current repo state:

```text
npm run verify:electron: passed
app protocol remains standard / secure / supportFetchAPI / stream
app protocol handler resolves through resolveAppProtocolPath()
forbidden paths return 403; missing files return 404
validated asset paths are served through net.fetch(pathToFileURL(filePath))
overlay and settings BrowserWindow keep contextIsolation: true, nodeIntegration: false, sandbox: true
overlay window remains non-focusable, skipTaskbar, mouse-event-through, and screen-saver always-on-top
settings window keeps menu bar hidden
app:// path resolver still limits access to assets/ and decodes encoded paths before validation
preload scripts only require electron and do not use remote
npx vitest run tests\app-protocol-path.test.js: passed, 6 tests
app://bundle/package.json is rejected
app://bundle/assets-other/025-pikachu.png is rejected
```

Interpretation:

- This adds regression coverage for the Electron security posture around the custom `app://` asset protocol and sandboxed renderer windows.
- It is a static guard, not a substitute for runtime penetration testing, but it prevents common high-risk drift while changing UI or packaging code.

## Settings UI Contract Check

Current repo state:

```text
npm run verify:settings: passed
settings.html contains enabled/search/grid/scale/scaleVal/offset/offsetVal/lerp/lerpVal IDs used by settings.js
scale input: number, min 0.5, max 5.0, step 0.05
offset input: number, min 0, max 100, step 1
lerp input: number, min 0.5, max 5.0, step 0.1
settings.js maps vcp1_* keys to main-process settings keys
settings.js defaults remain scale 1.25 / offset 70 / lerp 0.20
speed UI still converts lerp x10 on read and /10 on write
search still includes kana normalization, romaji, and dex-number terms
arrow controls still use native stepUp/stepDown
settings preload exposes getSettings / setSettings / listPacks only
```

Interpretation:

- This catches source-level settings-window contract drift that would otherwise show up only after opening the GUI.
- It does not prove visual layout or actual click behavior in Electron, but it guards the data/control surface used by the settings window.

## Issue #14: Generation 5-9 Assets

Current asset coverage:

```text
assets/packs/index.json entries: 493
assets/packs/index.json unique IDs: 493
National dex number range: 1-493, no missing numbers in 1-493
assets/packs/retro/gen-1 JSON: 151
assets/packs/retro/gen-2 JSON: 100
assets/packs/retro/gen-3 JSON: 135
assets/packs/retro/gen-4 JSON: 107
assets/ui/gen-1 PNG: 151
assets/ui/gen-2 PNG: 100
assets/ui/gen-3 PNG: 135
assets/ui/gen-4 PNG: 107
assets/packs/jp-names.json entries: 493, no missing numbers in 1-493
index <-> pack JSON: no missing or extra entries
index <-> UI PNG: no missing or extra entries
index <-> jp-names by dex number: no missing or extra entries
Gen 5 range 494-649: 0 / 156 present
Gen 6 range 650-721: 0 / 72 present
Gen 7 range 722-809: 0 / 88 present
Gen 8 range 810-905: 0 / 96 present
Gen 9 range 906-1025: 0 / 120 present
npm run verify:assets: passed for current Gen 1-4 assets
npx vitest run tests\asset-path.test.js: passed, 7 tests
asset-path generation boundaries covered: Gen 1 1-151, Gen 2 152-251, Gen 3 252-386, Gen 4 387-493, Gen 5 494-649, Gen 6 650-721, Gen 7 722-809, Gen 8 810-905, Gen 9 906+
generation inference candidates covered for 494, 650, 722, 810, 906
```

Interpretation:

- Current coverage is exactly Gen 1-4 / 493 entries.
- Gen 1-4 are internally consistent across index, pack JSON, UI PNG, and Japanese names.
- The new asset verifier also checks each pack's raw animation sheet references and required direction rows.
- The asset verifier also checks pack `generation`, pack `name`, duplicate dex numbers, index display-name dex prefix, and non-empty Japanese/romaji names.
- Gen 5-9 are not present in the current repository state. Number-range estimate: 532 missing entries across Gen 5-9, so Issue #14 remains open and confirmed.
- The helper logic for future Gen 5-9 pack-key inference is now covered even though the actual Gen 5-9 assets are still absent.
- `scripts/fetch-jp-names.cjs` still has `TOTAL = 493`, matching the issue's unchecked task to expand the fetch range to 1-1025.

Pack-list and search cost, without launching Electron:

```text
npm run bench:pack-list
[bench-pack-list] iterations: 1000
[bench-pack-list] current entries: 493
[bench-pack-list] target entries: 1025
[bench-pack-list] readPackList: 0.8656ms
[bench-pack-list] current search index: 0.8115ms
[bench-pack-list] current search loop: 28.2155ms
[bench-pack-list] synthetic 1025 list: 0.2328ms
[bench-pack-list] synthetic 1025 search index: 0.8078ms
[bench-pack-list] synthetic 1025 search loop: 40.0681ms
```

Interpretation:

- The data path behind the settings tile list is cheap on this Windows machine for the current 493 entries.
- A synthetic 1025-entry list also stays cheap for repeated search filtering in pure JS.
- This does not prove renderer layout, image decode, lazy-load behavior, or perceived scroll/search smoothness in the actual settings window. That remains a GUI validation item once Gen 5-9 sprites exist.

## Issue #16: Signing

Local Windows unpacked executable:

```text
Get-AuthenticodeSignature release\win-unpacked\PokeFollower.exe
Status: NotSigned
```

Published v1.0.2 Windows installer:

```text
Downloaded asset: PokeFollower-Setup.exe
Size: 119472756 bytes
GitHub Release digest: sha256:f43c36efd50302edab3052aa46cc7cab9064caf7cd0a58dd7fc54ae43edc9d95
Local SHA256: F43C36EFD50302EDAB3052AA46CC7CAB9064CAF7CD0A58DD7FC54AE43EDC9D95
Get-AuthenticodeSignature: NotSigned
VersionInfo ProductVersion: 1.0.2
VersionInfo FileVersion: 1.0.2
VersionInfo ProductName: PokeFollower
VersionInfo CompanyName: naochan3
```

Interpretation:

- The local smoke build and the published v1.0.2 Windows installer are unsigned.
- The downloaded installer hash matches the GitHub Release digest, so the `NotSigned` result applies to the published asset, not only a local rebuild.

Published v1.0.2 Windows zip static check:

```text
Downloaded asset: PokeFollower-1.0.2-win.zip
Size: 161391288 bytes
GitHub Release digest: sha256:984d402d1960c7e52d7ccd77823435fc5fda4790242cb40d5a598d786019fefc
Local SHA256: 984D402D1960C7E52D7CCD77823435FC5FDA4790242CB40D5A598D786019FEFC
Extracted executable: PokeFollower.exe, 232313344 bytes
Get-AuthenticodeSignature: NotSigned
VersionInfo ProductVersion: 1.0.2.0
VersionInfo FileVersion: 1.0.2
VersionInfo ProductName: PokeFollower
VersionInfo CompanyName: naochan3
app.asar package.json version: 1.0.2
app.asar contains: native/pokefollower_core.wasm
app.asar contains: @koromix/koffi-win32-x64
```

Interpretation:

- The published Windows zip hash matches the GitHub Release digest.
- The zip distribution carries the expected v1.0.2 app payload, WASM core, and win32-x64 native dependency.
- The zip distribution's executable is also unsigned, so Issue #16 applies to both the installer and zip assets.
- `PokeFollower.exe --version` is not a safe metadata-only check: it launched the packaged Electron app and created an HKCU Run entry through packaged startup behavior. The spawned processes were stopped, the temporary `electron.app.PokeFollower` Run value pointing at `AppData\Local\Temp\pokefollower-v102-versioninfo-check\extract\PokeFollower.exe` was removed, and the temp extraction directory was deleted.

Published v1.0.2 macOS zip static check:

```text
Downloaded asset: PokeFollower-1.0.2-arm64-mac.zip
Size: 136146247 bytes
GitHub Release digest: sha256:80ff580df2ec6fef14ea5a4325081c4bb45264cc91db6587b4383e11e2224fc4
Local SHA256: 80FF580DF2EC6FEF14EA5A4325081C4BB45264CC91DB6587B4383E11E2224FC4
Zip contains: PokeFollower.app/Contents/Resources/app.asar
Info.plist CFBundleShortVersionString: 1.0.2
Info.plist CFBundleVersion: 1.0.2
app.asar package.json version: 1.0.2
app.asar contains: native/pokefollower_core.wasm
app.asar contains: @koromix/koffi-darwin-arm64
MacOS/PokeFollower zip entry: 33968 bytes, Mach-O arm64 magic bytes CF FA ED FE 0C 00 00 01
_CodeSignature / CodeResources entries: not found
```

Interpretation:

- The published macOS zip hash matches the GitHub Release digest and the bundle carries v1.0.2 metadata.
- The macOS zip statically includes the WASM core and the darwin-arm64 native dependency.
- No code-signature bundle entries were found in the zip. This is consistent with Issue #16 remaining open.
- This does not prove macOS Gatekeeper behavior or runtime launch because it was inspected from Windows.

Published v1.0.2 macOS DMG digest check:

```text
Downloaded asset: PokeFollower-1.0.2-arm64.dmg
Size: 135998615 bytes
GitHub Release digest: sha256:efbefb1f918463156fcc540eac5670167e3cbc20136f3656adb654acddb172e3
Local SHA256: EFBEFB1F918463156FCC540EAC5670167E3CBC20136F3656ADB654ACDDB172E3
```

Latest release state:

```text
gh release view default/latest: v1.0.2, not draft, not prerelease
npm run verify:release: v1.0.2 is latest and has expected Windows/macOS assets with sha256 digests, README links, and no Linux AppImage
```

Interpretation:

- The published macOS DMG hash matches the GitHub Release digest.
- The repository latest release is v1.0.2, so README's Windows `releases/latest/download/PokeFollower-Setup.exe` points at v1.0.2.
- README's macOS links in this worktree point at the v1.0.2 arm64 DMG and zip assets.
- The local `verify:release` helper makes the latest-release, expected release-asset, and README-link check reproducible without downloading the assets.

Static signing configuration check:

```text
rg -n "certificate|certificateFile|certificateSubjectName|csc|CSC_|notarize|APPLE_|identity|Developer ID" package.json .github scripts docs README.md
matches only README / docs explanatory text
package.json build.win has no certificate configuration
package.json build.mac has no notarize / identity configuration
.github/workflows has no signing/notarization secrets or release signing job
npm run verify:signing: passed
```

Interpretation:

- Issue #16 is confirmed at both artifact and source-configuration levels: current published artifacts are unsigned, and the repository does not yet contain electron-builder signing/notarization configuration.

## Issue #17: Linux / macOS Runtime Validation

Additional package-smoke finding:

- `npm run dist:mac -- --arm64 --dir` on Windows fails with Electron Builder's expected `Build for macOS is supported only on macOS` error.
- `npm run dist:linux -- --dir` on Windows creates a Linux-shaped bundle, but package content verification fails for Linux target because the bundle contains `@koromix/koffi-win32-x64`, not `@koromix/koffi-linux-x64`.
- `package.json` has Linux AppImage target configured (`build.linux.target = ["AppImage"]`, `dist:linux = electron-builder --linux`).
- v1.0.2 Release assets are Windows installer, Windows zip, macOS arm64 DMG, and macOS arm64 zip only. No Linux AppImage asset is attached.
- README and STATUS describe Linux as AppImage build support only / real desktop behavior unverified, which matches the absence of a Linux release asset.
- `npm run verify:platform` passed, confirming:
  - `src/main/fullscreen-detect.js` defaults to a no-op and only enables foreground-window detection inside `process.platform === "win32"`.
  - README / STATUS explicitly state fullscreen auto-hide is Windows-only.
  - README / STATUS explicitly state Linux is AppImage build support only and runtime behavior still needs validation.
  - README / STATUS describe the published macOS package as arm64 / Apple Silicon, not Intel.
- `tests/fullscreen-policy.test.js` covers the pure fullscreen policy used by `main.js`:
  - missing foreground info is not fullscreen.
  - shell windows (`Progman`, `Shell_TrayWnd`) are excluded even when screen-sized.
  - auxiliary shell windows (`WorkerW`, `Shell_SecondaryTrayWnd`) and empty class names are excluded.
  - screen-sized foreground windows are fullscreen.
  - display `scaleFactor` is considered for DPI-scaled monitors.
  - normal maximized windows that do not cover the full monitor are not fullscreen.
  - 2px size tolerance is accepted and 3px shortfall is rejected.
- `npx vitest run tests\fullscreen-policy.test.js`: passed, 7 tests.

Interpretation:

- Linux and macOS package validation must run on native GitHub Actions runners or real machines.
- The new package smoke verifier should catch wrong native dependency packaging on `ubuntu-latest` and `macos-latest` after the workflow is pushed.
- The macOS smoke command now explicitly builds `--arm64` and verifies `darwin arm64`, matching the published v1.0.2 Apple Silicon assets.
- The package smoke verifier now also checks packaged `package.json` name/version/main against the repository root, so generated app payload version drift is caught before release.
- Issue #17 remains open at both levels: no Linux release artifact is currently published for v1.0.2, and runtime behavior still requires Linux desktop validation.

## Issue #19: macOS v1.0.2 Release Attachment

Current GitHub state:

```text
Issue #19: closed at 2026-06-18T07:19:00Z
v1.0.2 release assets:
- PokeFollower-1.0.2-arm64.dmg, sha256 efbefb1f918463156fcc540eac5670167e3cbc20136f3656adb654acddb172e3
- PokeFollower-1.0.2-arm64-mac.zip, sha256 80ff580df2ec6fef14ea5a4325081c4bb45264cc91db6587b4383e11e2224fc4
- PokeFollower-1.0.2-win.zip, sha256 984d402d1960c7e52d7ccd77823435fc5fda4790242cb40d5a598d786019fefc
- PokeFollower-Setup.exe, sha256 f43c36efd50302edab3052aa46cc7cab9064caf7cd0a58dd7fc54ae43edc9d95
npm run verify:release: passed
```

Historical README / docs drift found before local worktree fix:

```text
README macOS download link still points at releases/download/v1.0.1/PokeFollower-1.0.0-arm64.dmg
README still says macOS is v1.0.1 / Electron 31 and Electron 42 macOS build is in preparation.
docs/STATUS.md still says current version v1.0.1.
```

Release tag source check:

```text
GitHub Release v1.0.2 targetCommitish: main
Local tag v1.0.2: 8523245e2e1806990690e93a6e7be5ea1565b144
Tag commit: release: v1.0.2（Electron 42 セキュリティ更新を含む Windows 配布ビルド）
v1.0.2 package.json version: 1.0.2
v1.0.2 package.json electron: ^42.4.1
v1.0.2 README already points macOS links at v1.0.1 / PokeFollower-1.0.0-arm64.*
v1.0.2 README already says macOS is v1.0.1 / Electron 31 and Electron 42 macOS build is in preparation.
```

Historical release procedure drift found before local worktree fix:

```text
RELEASING.md says macOS README links include the versioned asset name and must be updated on each version bump.
Current README violates that rule after v1.0.2 macOS assets were uploaded.
```

Interpretation:

- Release attachment side of Issue #19 is verified complete.
- Before the local worktree fix below, documentation follow-up from the issue body was incomplete in both the `v1.0.2` tag and `origin/main`: README and STATUS still described the old macOS release state.

Local worktree follow-up:

```text
README macOS dmg link updated to releases/download/v1.0.2/PokeFollower-1.0.2-arm64.dmg
README macOS zip link updated to releases/download/v1.0.2/PokeFollower-1.0.2-arm64-mac.zip
README stale macOS v1.0.1 / Electron 31 warning removed
README Windows installer generated-file example updated from 1.0.1 to 1.0.2
RELEASING link rule clarified: Windows uses latest/download fixed asset; macOS uses versioned releases/download/v<ver> links
docs/STATUS.md current version updated to v1.0.2
docs/STATUS.md included-assets section updated to Windows installer/zip and macOS dmg/zip
npm run verify:docs: passed, including stale Windows installer example and RELEASING link-rule guards
npm run verify:release: passed against GitHub latest Release v1.0.2
```

Interpretation:

- The documentation drift is fixed in `C:\Users\ogosh\work\lab\pokefollower-desktop-workflow`.
- This is not yet reflected on GitHub because the worktree changes have not been committed or pushed.

## Windows Installer UX Gate

Current repo state:

```text
package.json build.win.target: nsis, zip
package.json build.nsis.oneClick: true
package.json build.nsis.perMachine: false
package.json build.nsis.createDesktopShortcut: true
package.json build.nsis.runAfterFinish: true
README: describes the installer as one-click / no intermediate selection screens / auto-install / tray resident
npm run verify:installer: passed
```

Interpretation:

- The `oneClick: true` installer behavior is now treated as intentional UX instead of an accidental NSIS regression.
- CI static checks and `npm run verify:local` both include `verify:installer`, so future changes to installer mode or README install wording fail before release.
- `npm run verify:ci` also checks that `verify:local` still bundles all static gates plus `npm test`, so local validation cannot silently drop a verifier.

## Repository Hygiene Gate

Current repo state:

```text
npm run verify:hygiene: passed
.gitignore includes node_modules/, target/, dist/, out/, build/, release/, *.exe
electron-builder output directory remains release
git ls-files release: no tracked files
git ls-files node_modules: no tracked files
git ls-files crates/follower_core/target: no tracked files
native/pokefollower_core.wasm remains tracked
package-lock.json remains tracked
```

Interpretation:

- This prevents generated `release/` output, local dependency folders, and Cargo target output from accidentally becoming part of the PR.
- It also protects the two files required by the validation/release flow: `package-lock.json` for `npm ci` and the committed WASM artifact used by package builds.

## Dependency Metadata Gate

Current repo state:

```text
Before local fix: package.json version was 1.0.2, package-lock root version was 1.0.0
package-lock root version updated to 1.0.2
npm run verify:deps: passed
package-lock root name/version matches package.json
package-lock packages[""] name/version/engines/dependencies/devDependencies match package.json
package.json engines.node remains >=22.12.0
CI NODE_VERSION remains 22.12.0
package-lock lockfileVersion remains 3
locked versions remain electron 42.4.1, electron-builder 26.15.3, vitest 4.1.9, koffi 3.0.2
```

Interpretation:

- This catches release-version drift between `package.json` and `package-lock.json`, which matters because CI and package smoke use `npm ci`.
- It also keeps the CI Node runtime aligned with the package engine floor and locks the core toolchain versions being validated.

## Roadmap Issue Link Gate

Current repo state:

```text
npm run verify:roadmap: passed
README links to docs/STATUS.md
STATUS current version and included-assets section remain v1.0.2
STATUS roadmap links #14, #15, #16, #17 with the expected titles and notes
STATUS completed-plan table keeps #1, #2, #3, #4, #6, #7, #8, #9, #10
STATUS known limitations keep Linux runtime-unverified, Windows-only fullscreen, and unsigned distribution notes
```

Interpretation:

- This keeps the repo-local roadmap aligned with the currently tracked open work in GitHub Issues.
- It prevents docs from silently dropping the remaining blockers while the automatic gates keep expanding.

## Runtime Guard Static Check

Current repo state:

```text
npm run verify:runtime: passed
single instance lock is requested before app.whenReady()
missing single instance lock quits before windows are created
app.setLoginItemSettings({ openAtLogin: true }) remains gated behind app.isPackaged
display-added / display-removed / display-metrics-changed rebuild overlays
AC and battery sim intervals default to 16ms, with POKEFOLLOWER_SIM_INTERVAL_MS=8 available as explicit opt-in
settings-store, follower-sim, and settings UI default offset remain 70
settings file path remains resolved Electron userData/settings.json, with isolated benchmark userData hooks gated to test/dev env
npm run verify:ipc: passed
sim loop hides overlays before polling screen.getCursorScreenPoint() when disabled or fullscreenActive
duplicate visible frames are skipped before renderer IPC
```

Interpretation:

- This does not launch the app or prove tray/runtime UX, but it catches source-level regressions in the startup and multi-display guardrails tied to Issues #3, #6, and #15.
- The packaged-only startup guard is especially important because development/runtime smoke checks must not leave HKCU Run entries behind.
- The disabled/fullscreen hot path now avoids cursor polling and simulation stepping after issuing the hide frame, which reduces background work while the mascot is hidden.

## Pack Reader Regression Coverage

Current repo state:

```text
tests/pack-reader.test.js:
  retro/025-pikachu resolves to retro/gen-1/025-pikachu
  readPackList returns 493 entries sorted by dex number
  first entry has Japanese name フシギダネ / English Bulbasaur
  last entry has Japanese name アルセウス / English Arceus
  missing pack key throws pack not found
npm test: passed, 36 tests
```

Interpretation:

- The settings window's pack list path now has direct regression coverage for index reading, Japanese-name join, dex sort order, and generation inference.
- This does not prove image rendering in the Electron settings window, but it catches the data-shape failures that would make the selector empty, unsorted, or lose localized names.

## GitHub Actions Validation Gate

Current repo state:

```text
.github/workflows/ci.yml:
  triggers: pull_request, push to main, workflow_dispatch
  permissions: contents: read
  concurrency: cancel-in-progress
  NODE_VERSION: 22.12.0
  jobs:
    static-checks: repo/static/docs/security/platform/signing/WASM gates
    unit-tests: ubuntu-latest, windows-latest, macos-latest
    rust-wasm-artifact: cargo fmt, build Rust WASM, git diff committed artifact
    package-smoke: win32 x64, darwin arm64, linux x64 --dir packages
npm run verify:ci: passed
npm run verify:local: passed, including 15 static gates and Vitest 45 tests
git diff --check: passed
```

Local Windows package proof for the same smoke gate:

```text
npm run dist:win -- --dir: passed
node scripts\verify-package-smoke.cjs win32 x64: passed
release\win-unpacked\resources\app.asar contains v1.0.2, native/pokefollower_core.wasm, app sources, representative assets, and /node_modules/@koromix/koffi-win32-x64
app.asar.unpacked contains node_modules\@koromix\koffi-win32-x64\win32_x64\koffi.node
```

Release-state proof:

```text
gh issue list --repo naochan3/pokefollower-desktop --state all --limit 30:
  open: #14, #15, #16, #17
  closed: #1, #2, #3, #4, #6, #7, #8, #9, #10, #13, #19
npm run verify:release: passed
latest release is v1.0.2 with Windows installer, Windows zip, macOS arm64 DMG, macOS arm64 zip, and no Linux AppImage
```

Interpretation:

- The repository now has a reproducible GitHub Actions gate for the automatic validation paths that were previously manual or local-only.
- The workflow covers the current open issues where static/package evidence is meaningful: asset coverage (#14), update-interval guardrails (#15), unsigned distribution status (#16), and macOS/Linux platform boundary docs/package smoke (#17).
- This still does not complete the P0 runtime validation: packaged GUI smoothness, Task Manager CPU/memory under cursor movement, multi-monitor DPI/hot-plug behavior, fullscreen game hide/restore, and native Linux/macOS desktop behavior still need real machine checks.
